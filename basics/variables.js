// 1. Can't define a variable more than once

let petName = 'Crabby';
//  Assigned a new value for the variable
petName = 'Katie'

console.log(petName);

// 2. There are rules related to variable names has to start with letters/dollar sign/underscore

// 3. Variable names cannot be reserved keywords